# Next.js frontend (production)
# Used by `next build` (production build) and `next start`.
# Replace placeholders with your barease-prod outputs after `sst deploy --stage barease-prod`.

# Public JSON for menu data (prod bucket)
NEXT_PUBLIC_MENU_JSON_URL=https://bar-ease-menu-barease-prod.s3.ap-northeast-1.amazonaws.com/menu.json

# Public JSON for makers list (optional; replace if you host it)
NEXT_PUBLIC_MAKERS_JSON_URL=https://cdn.example.com/makers.json

# Recommend API endpoint (barease-prod API)
# Example: paste `ApiEndpoint` output from SST and append /recommend
# e.g. https://xyz987.execute-api.ap-northeast-1.amazonaws.com/recommend
NEXT_PUBLIC_RECOMMEND_API=https://api.example.com/recommend

