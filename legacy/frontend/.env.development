# Next.js frontend (development)
# These values are used by `next dev` and by `NODE_ENV=development next build`.
# Replace placeholders with your barease-dev outputs after `sst deploy --stage barease-dev`.

# Public JSON for menu data (dev bucket)
NEXT_PUBLIC_MENU_JSON_URL=https://bar-ease-menu-barease-dev.s3.ap-northeast-1.amazonaws.com/menu.json

# Public JSON for makers list (optional; replace if you host it)
NEXT_PUBLIC_MAKERS_JSON_URL=https://cdn.dev.example.com/makers.json

# Recommend API endpoint (barease-dev API)
# Example: paste `ApiEndpoint` output from SST and append /recommend
# e.g. https://abc123.execute-api.ap-northeast-1.amazonaws.com/recommend
NEXT_PUBLIC_RECOMMEND_API=https://dev-api.example.com/recommend

